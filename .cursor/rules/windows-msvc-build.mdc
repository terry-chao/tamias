---
description: Windows 下用 MSVC 编译必须先加载 VS 开发者环境
alwaysApply: true
---

# Windows 编译：先 vcvars，再 cmake

本仓库 Windows 预设是 **MSVC + Ninja Multi-Config**（`CMakePresets.json` 的 `msvc` / `debug`）。Cursor 的 PowerShell **不会**自动带上 VS 的 `INCLUDE` / `LIB`。直接跑 `cmake --build` 时，`cl.exe` 会报：

```
fatal error C1083: 无法打开包括文件: “cstdint”: No such file or directory
```

这不是缺库、也不是 CMake 配错。**不要原命令重试**，先加载开发者环境。

## 要这样做

在仓库根目录用 PowerShell 包一层 `cmd /c`，先 `call vcvars64.bat`，再编译：

```
cmd /c "call `"C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat`" && cmake --build --preset debug --target tamias"
```

- 编译其他配置：把 `debug` 换成 `release` 或 `relwithdebinfo`
- 只编测试：`--target tamias_tests`
- 已在 `build/` 且环境已加载时，也可用：`ninja -f build-Debug.ninja bin/Debug/tamias.exe`
- `cmake --preset` 必须在**仓库根**执行，不要在 `build/` 里找 `CMakePresets.json`

VS 安装路径若变了，用 vswhere 查 `installationPath`，再调用其 `VC\Auxiliary\Build\vcvars64.bat`。

## 不要这样做

```
cmake --build --preset debug --target tamias
```

未 `call vcvars64.bat` 时，PowerShell 里的 `cl` 没有标准库路径。IDE 的 CMake Tools 有 `cmake.useVsDeveloperEnvironment: always`，**终端里的 agent 没有**。
